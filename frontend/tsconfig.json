{
  "compilerOptions": {
    "jsx": "react",
    "target": "ES2020", // ECMAScript
    "lib": ["ES2020", "DOM", "DOM.Iterable"], // 라이브러리 파일
    "module": "es2020", // 생성될 모듈 코드 설정
    "allowJs": true, // JavaScript 파일 컴파일 허용
    
    /* Bundler Mode */
    "moduleResolution": "bundler", // 모듈 분석 방법
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "paths": {
      "@/*": ["./src/*"],
      "@Common/*": ["./src/common/*"],
      "@Component/*": ["./src/component/*"],
      "@States/*": ["./src/states/*"],
    },
    "allowSyntheticDefaultImports": true, // 기본 출력이 없는 모듈로부터 기본 호출 허용

    /* Linting */
    "strict": true, // 모든 엄격 유형 검사 옵션 활성화

    /* 추가 검사 옵션 */
    "noUnusedLocals": false, // 사용하지 않은 로컬이 있는 경우, 오류 출력
    "noUnusedParameters": false, // 사용하지 않은 변수가 있는 경우, 오류 출력
    "noFallthroughCasesInSwitch": true // Switch문 오류 유형
    
  },
  "files": [], // 컴파일 파일 목록 지정
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.d.ts", "src/**/*.js", "src/**/*.jsx"], // 컴파일 파일 지정
  "exclude": ["node_modules"],
  "references": [
    { "path": "./tsconfig.node.json" }
  ]
}
